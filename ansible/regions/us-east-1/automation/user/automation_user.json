{
  "schemaVersion": "0.3",
  "description": "Apply mogam-user sudoers (no sudo allowed)",
  "parameters": {
    "InstanceId": {
      "type": "String",
      "description": "EC2 instance ID"
    }
  },
  "mainSteps": [
    {
      "name": "ApplyUserSudoers",
      "action": "aws:runCommand",
      "inputs": {
        "DocumentName": "AWS-RunShellScript",
        "InstanceIds": ["{{ InstanceId }}"],
        "Parameters": {
          "commands": [
            "echo \"$(cat /home/app/ansible/regions/us-east-1/automation/user/user.b64)\" | base64 -d | sudo tee /etc/sudoers.d/mogam-user > /dev/null",
            "sudo chmod 440 /etc/sudoers.d/mogam-user"
          ]
        }
      }
    }
  ]
}
